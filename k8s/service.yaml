apiVersion: v1
kind: Service
metadata:
  name: process-mcp-server
  labels:
    app: process-mcp-server
spec:
  type: ClusterIP
  selector:
    app: process-mcp-server
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
  sessionAffinity: None
